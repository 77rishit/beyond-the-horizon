/** Closing plate under the journey. */
export function SiteFooter() {
  return (
    <footer className="relative z-10 border-t border-cream/10 bg-ink px-6 py-14 md:px-12">
      <div className="mx-auto flex max-w-5xl flex-col items-center gap-3 text-center">
        <p className="font-display text-2xl tracking-[0.3em] text-cream md:text-3xl">
          BEYOND THE HORIZON
        </p>
        <p className="max-w-[46ch] text-sm font-light text-cream/60">
          An interactive exploration of depth, motion and modern web design.
        </p>
      </div>
    </footer>
  );
}
